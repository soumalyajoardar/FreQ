package com.gresseymusic.wave.ui.theme

/**
 * Legacy alias (M15). The FreQ type scale lives in [FreqTypography] with
 * identical Material3 slot names, so existing screens keep compiling
 * untouched. New code should import [FreqTypography] directly; M16 removes
 * this alias.
 */
val Typography = FreqTypography

